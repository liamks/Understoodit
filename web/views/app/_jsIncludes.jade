for library in jsIncludes.libraries
  script(src='/javascripts/lib/' + library + ".js")


for module in jsIncludes.modules
  script(src='/javascripts/modules/' + module + ".js") 

for template in jsIncludes.templates
  script(src='/javascripts/' + template + ".js") 

script(src='/javascripts/' + jsIncludes.app + ".js")