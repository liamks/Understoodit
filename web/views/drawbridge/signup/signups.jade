h1 Sign ups / Waitlist


form(action='/admin/invite-signup', method='post')
  input(type='hidden', name='token', value=token)
  input.btn.btn-primary(type='submit', value="Invite")

  div
    label(for='anEmail') Manually Invite:
    input(type='text', name='anEmail')

  table.table.table-bordered.table-striped
    thead
      tr
        th
        th Email
        th Sign up Time
        th.last Request URL
    tbody
      each signup in signups
        tr
          td
            input(type="checkbox", name='person', value=signup.email)
          td= signup.email
          td= new Date(signup.wl_time)
          if(signup.wl_requestURL )
            td
              a(href=signup.wl_requestURL)=signup.wl_requestURL.substring(0, 40) + '...'
          else
            td= signup.wl_requestURL 
